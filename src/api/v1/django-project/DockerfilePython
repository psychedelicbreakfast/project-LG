# Python実行環境のDockerイメージを指定
FROM python:3.9.4

# Pythonの標準出力、標準エラー出力をバッファに保存しないように設定
ENV PYTHONUNBUFFERED 1

# コンテナにcodeディレクトリを作成
RUN mkdir /code

# 作業ディレクトリを/code配下へ変更
WORKDIR /code

# ビルドコンテキストのファイルを/code配下へコピー
ADD . /code/

# requirements.txtへ記述したライブラリをインストール
RUN pip install -r ./code/django_rest_api/requirements.txt
